#!/bin/bash

#set -e
#set -x

#NODE_VERSION=$(node -v)

#echo "node version: $NODE_VERSION"
#echo ""

# Overrides default /usr/bin/js symlink to node (system)
# in order to use the one provided by nvm.

node
